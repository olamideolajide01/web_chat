export default function LoadingSpinner() {
  return (
    <div
      className={` flex justify-center border-t-transparent w-5 h-5 border-4 border-slate-800 border-solid rounded-full animate-spin`}
    ></div>
  );
}
